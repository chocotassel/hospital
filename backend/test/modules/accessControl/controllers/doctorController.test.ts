import { DoctorController } from '../../src/controllers/doctorController';

// 添加你的测试代码，例如：
test('DoctorController should have a viewSelf method', () => {
    expect(typeof DoctorController.viewSelf).toBe('function');
});
