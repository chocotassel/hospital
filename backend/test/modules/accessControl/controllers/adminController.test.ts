import { AdminController } from '../../src/controllers/adminController';

// 添加你的测试代码，例如：
test('AdminController should have a viewDepartment method', () => {
    expect(typeof AdminController.viewDepartment).toBe('function');
});
