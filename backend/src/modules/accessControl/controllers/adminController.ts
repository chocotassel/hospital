import { Context } from 'koa';

export class AdminController {
    public static async viewDepartment(ctx: Context) {
        // 获取并返回科室信息的代码
    }

    public static async modifyDepartment(ctx: Context) {
        // 修改科室信息的代码
    }

    // 同理，添加其他的方法
}
